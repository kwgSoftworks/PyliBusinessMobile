enum TopDialogType{
  success,
  failure
}

enum LeadingIconType{
  drawer,
  back_arrow,
}

enum SideBarItems{
  home,
  customer,
  payment,
  plans,
  add_ons,
  contracts,
  options,
  account
}

enum RegistrationStageEnum{
  step1,
  step2,
  step3,
  completed,
}